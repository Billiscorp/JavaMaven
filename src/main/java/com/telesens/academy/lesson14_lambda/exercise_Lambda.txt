ДЗ_14 Лямбда выражения.txt

# Задачи на вложенные классы и лямбда-выражения

=====================================================
Шилдт "Руководство для начинающих" 6 изд.
стр. 503-537 - "Лямбда-выражения"
=====================================================

---------------  ОБЯЗАТЕЛЬНО ---------------
1) 	Составьте лямбда-выражение, которое возвращает значение true, если
		число принадлежит к диапазону чисел 10-20, включая граничные значения

2)	Создайте блочное лямбда-выражение для вычисления факториала целого числа.
		Продемонстрируйте его использование.

3)  Создайте лямбда-выражение, которое переводит строку в верхний режим.
		Перевести строку в верхний регистр с помощью лямбда-выражения.
		Перевести строку в верхний регистр с помощью ссылки на метод.

4) Заданную строку
	- разбить на слова
	- отсортировать слова по алфавиту не учитывая регистр первой буквы
	- отсортировать слова по длине
	- отсортировать слова по последней букве

5) Создайте лямбда-выражение, которое удаляет все пробелы из заданной строки и возвращает результат.
	Продемонстрируйте работу лямбда-выражения.

6) Отсортировать коллекцию лист List<EntityDress> (см. прошлое занятие), используя лямбда-выражения:
	a) по полю model
	b) по поля price
	c) по поля price по убыванию

7) Для класса GenArray (из предыдущих занятий):
	a) параметризовать типом EntityDress
	b) поместить 5 позиций EntityDress
	c) реализовать возможность перебора элементов циклом for each
	d) перебрать в цикле все элементы и вывести на экран

8) Реализовать Iterable для класса Words (из презентации), не используя вложенный класс













